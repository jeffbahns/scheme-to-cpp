
;(define (literal_ident_return return_me_please)
;  return_me_please
;  )

;(define (literal_quotedlit_return)
;  (if 0
;      5
;      6
;  )

;(define (returns_number)
;  666
;  )

(define (opraw t)
  (if (car (cdr t))
      (display 'hello_world)
      )
  )

(define (main)
  0
  )
